# Docker-app
App created using mongo db container and converting whole app into an docker image to run on several machine


do installl node modules by using command 
npm install
npm install express mongodb
